Command line FPGA flash version reader
This simple app reads back the FPGA version, and displays it


build instructions:

make


usage:
./FPGAVersion

Displays a simple string such as "FPGA Firmware version = 27"

